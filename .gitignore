# Python
.venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Secrets / state / user-data — never commit
.env
.env.local
.env.*.local
transcripts.db
transcripts.db-journal
recordings/

# local Silero VAD weights (onnx ~2.3MB) — fetched at setup, not committed
models/

# macOS
.DS_Store

# IDE
.vscode/
.idea/

# daily_record 本地工作日记,不进开源
docs/done/

# Logs
*.log

# 设计探索产物(mockup/截图/zip)+ 工具状态 — 不进 git
tui-terminal*
.omx/
